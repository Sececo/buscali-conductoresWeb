# Origen del backend (solo host, SIN /api/v1). Ej: http://localhost:3000 o https://buscali-backend.onrender.com
VITE_API_URL=http://localhost:3000
# true = peticiones relativas /api… (proxy Vite en dev y preview; recomendado en local).
VITE_USE_PROXY=true
